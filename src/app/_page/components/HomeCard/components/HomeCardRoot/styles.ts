import { tv } from 'tailwind-variants';

export const HomeCardRootStyles = tv({
  base: 'w-full animate-fade-in-slide-up rounded-[1.25rem] border border-neutral-900/[8%] bg-neutral-100 shadow-[0_2px_3px_0_rgba(20,27,52,0.06)]',
});
